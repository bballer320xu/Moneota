<% if user_signed_in? %>
  <%=t('menu.signed_in')%>
	<%= bar_break %>
 	<%= link_to t('menu.signout'), destroy_user_session_path %>
<% else %>
  <%= link_to t('menu.signup'), new_user_registration_path %>
	<%= bar_break %>
  <%= link_to t('menu.signin'), new_user_session_path %>
<% end %>